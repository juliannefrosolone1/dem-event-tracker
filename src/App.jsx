import { useState, useEffect, useMemo } from "react";

const CANDIDATES = [
  { id: "aoc",       name: "Alexandria Ocasio-Cortez", short: "AOC",       color: "#e63946" },
  { id: "newsom",    name: "Gavin Newsom",              short: "Newsom",    color: "#457b9d" },
  { id: "harris",    name: "Kamala Harris",             short: "Harris",    color: "#1a6bff" },
  { id: "pritzker",  name: "J.B. Pritzker",             short: "Pritzker",  color: "#e9c46a" },
  { id: "kelly",     name: "Mark Kelly",                short: "Kelly",     color: "#2a9d8f" },
  { id: "booker",    name: "Cory Booker",               short: "Booker",    color: "#f4a261" },
  { id: "shapiro",   name: "Josh Shapiro",              short: "Shapiro",   color: "#9b5de5" },
  { id: "khanna",    name: "Ro Khanna",                 short: "Khanna",    color: "#06d6a0" },
  { id: "crow",      name: "Jason Crow",                short: "Crow",      color: "#ff6b6b" },
  { id: "slotkin",   name: "Elissa Slotkin",            short: "Slotkin",   color: "#ffd166" },
  { id: "murphy",    name: "Chris Murphy",              short: "Murphy",    color: "#118ab2" },
  { id: "buttigieg", name: "Pete Buttigieg",            short: "Buttigieg", color: "#ff9f1c" },
  { id: "ossoff",    name: "Jon Ossoff",                short: "Ossoff",    color: "#b5e48c" },
];
const CANDIDATE_MAP = Object.fromEntries(CANDIDATES.map(c => [c.id, c]));
const EVENT_TYPES = [
  { id: "rally",            label: "Rally",            icon: "📣" },
  { id: "town_hall",        label: "Town Hall",         icon: "🏛️" },
  { id: "speech",           label: "Speech",            icon: "🎤" },
  { id: "fundraiser",       label: "Fundraiser",        icon: "💰" },
  { id: "interview",        label: "Interview",         icon: "🎙️" },
  { id: "media_appearance", label: "Media Appearance",  icon: "📺" },
  { id: "endorsement",      label: "Endorsement",       icon: "✅" },
  { id: "travel",           label: "Travel",            icon: "✈️" },
  { id: "other",            label: "Other",             icon: "📌" },
];
const EVENT_TYPE_MAP = Object.fromEntries(EVENT_TYPES.map(t => [t.id, t]));
const EARLY_STATES = ["IA", "NH", "NV", "SC", "GA"];
const ALL_STATES = ["AL","AK","AZ","AR","CA","CO","CT","DE","FL","GA","HI","ID","IL","IN","IA","KS","KY","LA","ME","MD","MA","MI","MN","MS","MO","MT","NE","NV","NH","NJ","NM","NY","NC","ND","OH","OK","OR","PA","RI","SC","SD","TN","TX","UT","VT","VA","WA","WV","WI","WY","DC"];
const STATE_NAMES = {AL:"Alabama",AK:"Alaska",AZ:"Arizona",AR:"Arkansas",CA:"California",CO:"Colorado",CT:"Connecticut",DE:"Delaware",FL:"Florida",GA:"Georgia",HI:"Hawaii",ID:"Idaho",IL:"Illinois",IN:"Indiana",IA:"Iowa",KS:"Kansas",KY:"Kentucky",LA:"Louisiana",ME:"Maine",MD:"Maryland",MA:"Massachusetts",MI:"Michigan",MN:"Minnesota",MS:"Mississippi",MO:"Missouri",MT:"Montana",NE:"Nebraska",NV:"Nevada",NH:"New Hampshire",NJ:"New Jersey",NM:"New Mexico",NY:"New York",NC:"North Carolina",ND:"North Dakota",OH:"Ohio",OK:"Oklahoma",OR:"Oregon",PA:"Pennsylvania",RI:"Rhode Island",SC:"South Carolina",SD:"South Dakota",TN:"Tennessee",TX:"Texas",UT:"Utah",VT:"Vermont",VA:"Virginia",WA:"Washington",WV:"West Virginia",WI:"Wisconsin",WY:"Wyoming",DC:"D.C."};
const SIGNIFICANCE_COLORS = { high: "#e63946", medium: "#f4a261", low: "#555" };
const TABS = ["Overview", "All Events", "By Candidate", "By State", "Press Clips"];
const LS_KEY = "event_tracker_manual";
function loadManualEvents() { try { return JSON.parse(localStorage.getItem(LS_KEY) || "[]"); } catch { return []; } }
function saveManualEvents(events) { localStorage.setItem(LS_KEY, JSON.stringify(events)); }
function blankEvent() { return { id: `manual-${Date.now()}`, candidate: "", date: new Date().toISOString().slice(0, 10), event_type: "other", title: "", location_city: "", location_state: "", location_display: "", venue: "", description: "", significance: "medium", press_clips: [], _manual: true }; }
const S = { app: { minHeight: "100vh", background: "#09090f", color: "#e8e8f0", fontFamily: "'Inter', system-ui, sans-serif" }, header: { padding: "32px 36px 24px", borderBottom: "1px solid #1a1a22" }, title: { fontSize: 28, fontWeight: 800, color: "#fff", margin: 0, letterSpacing: "-0.5px" }, subtitle: { fontSize: 13, color: "#555", fontFamily: "monospace", marginTop: 6 }, tabs: { display: "flex", gap: 0, padding: "0 36px", borderBottom: "1px solid #1a1a22" }, tab: (active) => ({ padding: "14px 20px", background: "transparent", border: "none", borderBottom: active ? "2px solid #1a6bff" : "2px solid transparent", color: active ? "#1a6bff" : "#666", cursor: "pointer", fontSize: 13, fontWeight: active ? 600 : 400, letterSpacing: "0.02em", transition: "color 0.15s" }), body: { padding: "28px 36px" }, card: { background: "#111118", border: "1px solid #1a1a22", borderRadius: 8, padding: "20px 24px", marginBottom: 12 }, statCard: { background: "#111118", border: "1px solid #1a1a22", borderRadius: 8, padding: "20px 24px" }, btn: (color = "#1a6bff") => ({ background: color, border: "none", borderRadius: 6, color: "#fff", padding: "8px 18px", cursor: "pointer", fontSize: 13, fontWeight: 600 }), btnOutline: { background: "transparent", border: "1px solid #333", borderRadius: 6, color: "#aaa", padding: "7px 16px", cursor: "pointer", fontSize: 13 }, badge: (color) => ({ display: "inline-block", background: `${color}22`, color: color, border: `1px solid ${color}44`, borderRadius: 4, padding: "2px 8px", fontSize: 11, fontWeight: 600, letterSpacing: "0.04em" }), input: { background: "#0d0d14", border: "1px solid #2a2a35", borderRadius: 6, color: "#e8e8f0", padding: "8px 12px", fontSize: 13, width: "100%", boxSizing: "border-box" }, label: { fontSize: 12, color: "#666", marginBottom: 4, display: "block" }, grid3: { display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 16 } };
function CandidateBadge({ id }) { const c = CANDIDATE_MAP[id]; if (!c) return <span style={{ color: "#555", fontSize: 12 }}>{id}</span>; return <span style={S.badge(c.color)}>{c.short}</span>; }
function EventTypeBadge({ type }) { const t = EVENT_TYPE_MAP[type] || { label: type, icon: "📌" }; return <span style={{ fontSize: 12, color: "#888", display: "inline-flex", alignItems: "center", gap: 4 }}><span>{t.icon}</span>{t.label}</span>; }
function SignificanceDot({ sig }) { return <span style={{ display: "inline-block", width: 8, height: 8, borderRadius: "50%", background: SIGNIFICANCE_COLORS[sig] || "#555", marginRight: 4 }} />; }
function PressClips({ clips }) { if (!clips || clips.length === 0) return null; return <div style={{ marginTop: 10, paddingTop: 10, borderTop: "1px solid #1a1a22" }}><div style={{ fontSize: 11, color: "#555", marginBottom: 6, fontFamily: "monospace" }}>PRESS CLIPS</div>{clips.map((clip, i) => <div key={i} style={{ fontSize: 12, marginBottom: 4, display: "flex", gap: 8, alignItems: "flex-start" }}><span style={{ color: "#444", minWidth: 90, flexShrink: 0 }}>{clip.outlet}</span>{clip.url ? <a href={clip.url} target="_blank" rel="noopener noreferrer" style={{ color: "#6a9bff", textDecoration: "none" }}>{clip.headline}</a> : <span style={{ color: "#888" }}>{clip.headline}</span>}{clip.date && <span style={{ color: "#444", fontSize: 11, whiteSpace: "nowrap" }}>{clip.date}</span>}</div>)}</div>; }
function EventCard({ event, onEdit, onDelete }) { const [expanded, setExpanded] = useState(false); const c = CANDIDATE_MAP[event.candidate]; return <div style={{ ...S.card, borderLeft: `3px solid ${c?.color || "#333"}` }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}><div style={{ flex: 1 }}><div style={{ display: "flex", gap: 8, alignItems: "center", flexWrap: "wrap", marginBottom: 6 }}><CandidateBadge id={event.candidate} /><span style={{ fontSize: 12, color: "#555", fontFamily: "monospace" }}>{event.date}</span><EventTypeBadge type={event.event_type} /><SignificanceDot sig={event.significance} />{event._manual && <span style={{ fontSize: 10, color: "#444", fontFamily: "monospace" }}>MANUAL</span>}</div><div style={{ fontSize: 15, fontWeight: 600, color: "#e8e8f0", marginBottom: 4 }}>{event.title}</div><div style={{ fontSize: 12, color: "#666" }}>{event.location_display || `${event.location_city}${event.location_state ? ", " + event.location_state : ""}`}{event.venue && ` · ${event.venue}`}</div></div><div style={{ display: "flex", gap: 8, marginLeft: 16, flexShrink: 0 }}>{event.press_clips?.length > 0 && <button onClick={() => setExpanded(e => !e)} style={S.btnOutline}>{expanded ? "▲" : `${event.press_clips.length} clip${event.press_clips.length !== 1 ? "s" : ""}`}</button>}<button onClick={() => onEdit(event)} style={S.btnOutline}>Edit</button><button onClick={() => onDelete(event.id)} style={{ ...S.btnOutline, color: "#e63946", borderColor: "#e6394644" }}>✕</button></div></div>{event.description && <div style={{ fontSize: 13, color: "#888", marginTop: 8, lineHeight: 1.5 }}>{event.description}</div>}{expanded && <PressClips clips={event.press_clips} />}</div>; }
function EventForm({ event, onSave, onCancel }) { const [form, setForm] = useState({ ...event }); const [clipText, setClipText] = useState(""); const set = (k, v) => setForm(f => ({ ...f, [k]: v })); function addClip() { const parts = clipText.split("|").map(s => s.trim()); if (parts.length < 2) return; const [outlet, headline, url = "", date = ""] = parts; setForm(f => ({ ...f, press_clips: [...(f.press_clips || []), { outlet, headline, url, date }] })); setClipText(""); } function removeClip(i) { setForm(f => ({ ...f, press_clips: f.press_clips.filter((_, idx) => idx !== i) })); } const locationDisplay = form.location_city ? `${form.location_city}${form.location_state ? ", " + form.location_state : ""}` : form.location_display || ""; function handleSave() { if (!form.candidate || !form.date || !form.title) { alert("Candidate, date, and title are required."); return; } onSave({ ...form, location_display: locationDisplay, _manual: true }); } return <div style={{ background: "#0d0d14", border: "1px solid #2a2a35", borderRadius: 10, padding: 24, marginBottom: 20 }}><div style={{ fontSize: 16, fontWeight: 700, color: "#fff", marginBottom: 20 }}>{event.id.startsWith("manual-") && event.title === "" ? "Add New Event" : "Edit Event"}</div><div style={S.grid3}><div><label style={S.label}>Candidate *</label><select value={form.candidate} onChange={e => set("candidate", e.target.value)} style={S.input}><option value="">Select candidate…</option>{CANDIDATES.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div><label style={S.label}>Date *</label><input type="date" value={form.date} onChange={e => set("date", e.target.value)} style={S.input} /></div><div><label style={S.label}>Event Type</label><select value={form.event_type} onChange={e => set("event_type", e.target.value)} style={S.input}>{EVENT_TYPES.map(t => <option key={t.id} value={t.id}>{t.icon} {t.label}</option>)}</select></div></div><div style={{ marginTop: 14 }}><label style={S.label}>Title *</label><input value={form.title} onChange={e => set("title", e.target.value)} style={S.input} placeholder="Event title…" /></div><div style={{ ...S.grid3, marginTop: 14 }}><div><label style={S.label}>City</label><input value={form.location_city} onChange={e => set("location_city", e.target.value)} style={S.input} placeholder="Des Moines" /></div><div><label style={S.label}>State</label><input value={form.location_state} onChange={e => set("location_state", e.target.value.toUpperCase().slice(0,2))} style={S.input} placeholder="IA" maxLength={2} /></div><div><label style={S.label}>Venue</label><input value={form.venue} onChange={e => set("venue", e.target.value)} style={S.input} placeholder="Venue name…" /></div></div><div style={{ marginTop: 14 }}><label style={S.label}>Description</label><textarea value={form.description} onChange={e => set("description", e.target.value)} style={{ ...S.input, height: 80, resize: "vertical" }} placeholder="Brief description of the event…" /></div><div style={{ marginTop: 14 }}><label style={S.label}>Significance</label><select value={form.significance} onChange={e => set("significance", e.target.value)} style={{ ...S.input, width: "auto" }}><option value="high">● High</option><option value="medium">● Medium</option><option value="low">● Low</option></select></div><div style={{ marginTop: 20 }}><label style={{ ...S.label, marginBottom: 8, fontSize: 13, color: "#aaa" }}>Press Clips</label>{form.press_clips?.map((clip, i) => <div key={i} style={{ display: "flex", gap: 8, marginBottom: 6, alignItems: "center" }}><span style={{ fontSize: 12, color: "#888", flex: 1 }}>{clip.outlet} · {clip.headline}</span><button onClick={() => removeClip(i)} style={{ ...S.btnOutline, fontSize: 11, padding: "4px 10px", color: "#e63946" }}>✕</button></div>)}<div style={{ display: "flex", gap: 8, marginTop: 8 }}><input value={clipText} onChange={e => setClipText(e.target.value)} style={{ ...S.input, flex: 1 }} placeholder="Outlet | Headline | URL (optional) | Date (optional)" onKeyDown={e => e.key === "Enter" && addClip()} /><button onClick={addClip} style={S.btn()}>Add</button></div><div style={{ fontSize: 11, color: "#444", marginTop: 4 }}>Format: Outlet | Headline | URL | Date — press Enter or click Add</div></div><div style={{ display: "flex", gap: 10, marginTop: 24 }}><button onClick={handleSave} style={S.btn()}>Save Event</button><button onClick={onCancel} style={S.btnOutline}>Cancel</button></div></div>; }
function OverviewTab({ events }) { const stats = useMemo(() => CANDIDATES.map(c => { const ce = events.filter(e => e.candidate === c.id); const earlyState = ce.filter(e => EARLY_STATES.includes(e.location_state)).length; const clips = ce.reduce((sum, e) => sum + (e.press_clips?.length || 0), 0); const highSig = ce.filter(e => e.significance === "high").length; return { ...c, total: ce.length, earlyState, clips, highSig }; }).sort((a, b) => b.total - a.total), [events]); const maxTotal = Math.max(...stats.map(s => s.total), 1); return <div><div style={{ fontSize: 11, color: "#555", fontFamily: "monospace", marginBottom: 20 }}>{events.length} total events tracked across {CANDIDATES.length} candidates</div><div style={S.card}><div style={{ fontSize: 13, fontWeight: 600, color: "#aaa", marginBottom: 20 }}>EVENT COUNT BY CANDIDATE</div>{stats.map(s => <div key={s.id} style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 10 }}><div style={{ width: 72, fontSize: 12, color: s.color, textAlign: "right", flexShrink: 0 }}>{s.short}</div><div style={{ flex: 1, background: "#1a1a22", borderRadius: 4, height: 22, position: "relative", overflow: "hidden" }}><div style={{ width: `${(s.total / maxTotal) * 100}%`, background: `${s.color}55`, borderRight: `2px solid ${s.color}`, height: "100%", transition: "width 0.5s", minWidth: s.total > 0 ? 6 : 0 }} /></div><div style={{ width: 28, fontSize: 13, fontWeight: 600, color: "#fff", textAlign: "right", fontFamily: "monospace" }}>{s.total}</div>{s.earlyState > 0 && <div style={{ fontSize: 11, color: "#2a9d8f", fontFamily: "monospace", width: 40 }}>★ {s.earlyState}</div>}</div>)}<div style={{ fontSize: 11, color: "#444", marginTop: 8, fontFamily: "monospace" }}>★ = events in early primary states (IA, NH, NV, SC, GA)</div></div><div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12, marginTop: 16 }}>{[{ label: "Total Events", value: events.length }, { label: "Early State Visits", value: events.filter(e => EARLY_STATES.includes(e.location_state)).length }, { label: "Press Clips", value: events.reduce((s, e) => s + (e.press_clips?.length || 0), 0) }, { label: "High Significance", value: events.filter(e => e.significance === "high").length }].map(s => <div key={s.label} style={S.statCard}><div style={{ fontSize: 28, fontWeight: 800, color: "#fff" }}>{s.value}</div><div style={{ fontSize: 11, color: "#555", fontFamily: "monospace", marginTop: 4 }}>{s.label.toUpperCase()}</div></div>)}</div><div style={{ marginTop: 24 }}><div style={{ fontSize: 13, fontWeight: 600, color: "#aaa", marginBottom: 14 }}>RECENT HIGH-SIGNIFICANCE EVENTS</div>{events.filter(e => e.significance === "high").slice(0, 5).map(e => { const c = CANDIDATE_MAP[e.candidate]; return <div key={e.id} style={{ ...S.card, borderLeft: `3px solid ${c?.color || "#333"}`, padding: "14px 18px" }}><div style={{ display: "flex", gap: 10, alignItems: "center" }}><CandidateBadge id={e.candidate} /><span style={{ fontSize: 12, color: "#555", fontFamily: "monospace" }}>{e.date}</span><span style={{ fontSize: 13, fontWeight: 600 }}>{e.title}</span><span style={{ fontSize: 12, color: "#666" }}>· {e.location_display}</span></div></div>; })}{events.filter(e => e.significance === "high").length === 0 && <div style={{ color: "#444", fontSize: 13 }}>No high-significance events yet.</div>}</div></div>; }
function AllEventsTab({ events, onEdit, onDelete, onAdd }) { const [filterCandidate, setFilterCandidate] = useState("all"); const [filterType, setFilterType] = useState("all"); const [filterSig, setFilterSig] = useState("all"); const [search, setSearch] = useState(""); const filtered = useMemo(() => events.filter(e => { if (filterCandidate !== "all" && e.candidate !== filterCandidate) return false; if (filterType !== "all" && e.event_type !== filterType) return false; if (filterSig !== "all" && e.significance !== filterSig) return false; if (search) { const q = search.toLowerCase(); return e.title?.toLowerCase().includes(q) || e.description?.toLowerCase().includes(q) || e.location_display?.toLowerCase().includes(q) || e.venue?.toLowerCase().includes(q); } return true; }), [events, filterCandidate, filterType, filterSig, search]); return <div><div style={{ display: "flex", gap: 10, marginBottom: 20, flexWrap: "wrap", alignItems: "center" }}><input value={search} onChange={e => setSearch(e.target.value)} style={{ ...S.input, width: 200 }} placeholder="Search events…" /><select value={filterCandidate} onChange={e => setFilterCandidate(e.target.value)} style={{ ...S.input, width: "auto" }}><option value="all">All Candidates</option>{CANDIDATES.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={filterType} onChange={e => setFilterType(e.target.value)} style={{ ...S.input, width: "auto" }}><option value="all">All Types</option>{EVENT_TYPES.map(t => <option key={t.id} value={t.id}>{t.icon} {t.label}</option>)}</select><select value={filterSig} onChange={e => setFilterSig(e.target.value)} style={{ ...S.input, width: "auto" }}><option value="all">All Significance</option><option value="high">● High</option><option value="medium">● Medium</option><option value="low">● Low</option></select><div style={{ marginLeft: "auto" }}><button onClick={onAdd} style={S.btn()}>+ Add Event</button></div></div><div style={{ fontSize: 12, color: "#555", fontFamily: "monospace", marginBottom: 14 }}>Showing {filtered.length} of {events.length} events</div>{filtered.length === 0 ? <div style={{ color: "#444", fontSize: 13 }}>No events match your filters.</div> : filtered.map(e => <EventCard key={e.id} event={e} onEdit={onEdit} onDelete={onDelete} />)}</div>; }
function ByCandidateTab({ events, onEdit, onDelete, onAdd }) { const [selectedId, setSelectedId] = useState(CANDIDATES[0].id); const cEvents = useMemo(() => events.filter(e => e.candidate === selectedId), [events, selectedId]); const candidate = CANDIDATE_MAP[selectedId]; const stateCounts = useMemo(() => { const counts = {}; cEvents.forEach(e => { if (e.location_state) counts[e.location_state] = (counts[e.location_state] || 0) + 1; }); return Object.entries(counts).sort((a,b) => b[1]-a[1]); }, [cEvents]); return <div><div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 24 }}>{CANDIDATES.map(c => { const count = events.filter(e => e.candidate === c.id).length; return <button key={c.id} onClick={() => setSelectedId(c.id)} style={{ background: selectedId === c.id ? `${c.color}22` : "#111118", border: `1px solid ${selectedId === c.id ? c.color : "#2a2a35"}`, color: selectedId === c.id ? c.color : "#666", borderRadius: 6, padding: "8px 16px", cursor: "pointer", fontSize: 12, fontWeight: selectedId === c.id ? 700 : 400 }}>{c.short}<span style={{ marginLeft: 6, opacity: 0.7 }}>{count}</span></button>; })}</div><div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12, marginBottom: 20 }}>{[{ label: "Total Events", value: cEvents.length }, { label: "Early State Visits", value: cEvents.filter(e => EARLY_STATES.includes(e.location_state)).length }, { label: "Press Clips", value: cEvents.reduce((s, e) => s + (e.press_clips?.length || 0), 0) }, { label: "States Visited", value: new Set(cEvents.map(e => e.location_state).filter(Boolean)).size }].map(s => <div key={s.label} style={{ ...S.statCard, borderTop: `2px solid ${candidate.color}` }}><div style={{ fontSize: 24, fontWeight: 800, color: candidate.color }}>{s.value}</div><div style={{ fontSize: 11, color: "#555", fontFamily: "monospace", marginTop: 4 }}>{s.label.toUpperCase()}</div></div>)}</div>{stateCounts.length > 0 && <div style={{ ...S.card, marginBottom: 20 }}><div style={{ fontSize: 12, color: "#555", fontFamily: "monospace", marginBottom: 12 }}>WHERE THEY'VE BEEN</div><div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>{stateCounts.map(([state, count]) => <div key={state} style={{ background: EARLY_STATES.includes(state) ? `${candidate.color}22` : "#1a1a22", border: `1px solid ${EARLY_STATES.includes(state) ? candidate.color + "55" : "#2a2a35"}`, borderRadius: 6, padding: "8px 14px", textAlign: "center" }}><div style={{ fontSize: 16, fontWeight: 700, color: EARLY_STATES.includes(state) ? candidate.color : "#e8e8f0" }}>{state}</div><div style={{ fontSize: 11, color: "#555" }}>{count} event{count !== 1 ? "s" : ""}</div>{EARLY_STATES.includes(state) && <div style={{ fontSize: 10, color: candidate.color, marginTop: 2 }}>EARLY STATE</div>}</div>)}</div></div>}<div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 14 }}><div style={{ fontSize: 13, color: "#aaa", fontWeight: 600 }}>{candidate.name} · {cEvents.length} event{cEvents.length !== 1 ? "s" : ""}</div><button onClick={onAdd} style={S.btn(candidate.color)}>+ Add Event</button></div>{cEvents.length === 0 ? <div style={{ color: "#444", fontSize: 13 }}>No events tracked for {candidate.name} yet.</div> : cEvents.map(e => <EventCard key={e.id} event={e} onEdit={onEdit} onDelete={onDelete} />)}</div>; }
function ByStateTab({ events, onEdit, onDelete }) { const [selectedState, setSelectedState] = useState(null); const [showOnlyActive, setShowOnlyActive] = useState(true); const stateCounts = useMemo(() => { const counts = {}; events.forEach(e => { if (e.location_state) counts[e.location_state] = (counts[e.location_state] || 0) + 1; }); return counts; }, [events]); const activeStates = ALL_STATES.filter(s => stateCounts[s]); const displayStates = showOnlyActive ? activeStates : ALL_STATES; const stateEvents = useMemo(() => selectedState ? events.filter(e => e.location_state === selectedState).sort((a,b) => b.date.localeCompare(a.date)) : [], [events, selectedState]); const totalStates = activeStates.length; const totalEvents = events.filter(e => e.location_state).length; return <div><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}><div style={{ fontSize: 12, color: "#555", fontFamily: "monospace" }}>{totalEvents} events across {totalStates} state{totalStates !== 1 ? "s" : ""}</div><button onClick={() => setShowOnlyActive(v => !v)} style={S.btnOutline}>{showOnlyActive ? "Show all 50 states" : "Show active only"}</button></div><div style={{ ...S.card, marginBottom: 24 }}><div style={{ fontSize: 12, color: "#555", fontFamily: "monospace", marginBottom: 16 }}>CLICK A STATE TO SEE EVENTS · <span style={{ color: "#2a9d8f" }}>★ = early primary state</span></div><div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>{displayStates.map(state => { const count = stateCounts[state] || 0; const isEarly = EARLY_STATES.includes(state); const isSelected = selectedState === state; return <button key={state} onClick={() => setSelectedState(isSelected ? null : state)} disabled={count === 0} style={{ background: isSelected ? (isEarly ? "#2a9d8f33" : "#1a6bff22") : count > 0 ? "#1a1a22" : "#0d0d14", border: `1px solid ${isSelected ? (isEarly ? "#2a9d8f" : "#1a6bff") : isEarly && count > 0 ? "#2a9d8f66" : count > 0 ? "#2a2a35" : "#161616"}`, borderRadius: 6, padding: "8px 10px", cursor: count > 0 ? "pointer" : "default", minWidth: 52, textAlign: "center" }}><div style={{ fontSize: 12, fontWeight: 700, color: isSelected ? (isEarly ? "#2a9d8f" : "#1a6bff") : count > 0 ? "#e8e8f0" : "#2a2a35" }}>{isEarly ? "★ " : ""}{state}</div><div style={{ fontSize: 10, color: count > 0 ? "#888" : "#222", fontFamily: "monospace" }}>{count > 0 ? count : "—"}</div></button>; })}</div></div>{selectedState && <div><div style={{ fontSize: 16, fontWeight: 700, color: "#fff", marginBottom: 14, display: "flex", alignItems: "center", gap: 10 }}><span style={S.badge(EARLY_STATES.includes(selectedState) ? "#2a9d8f" : "#1a6bff")}>{selectedState}</span><span>{STATE_NAMES[selectedState]}</span>{EARLY_STATES.includes(selectedState) && <span style={{ fontSize: 11, color: "#2a9d8f", fontFamily: "monospace" }}>EARLY PRIMARY STATE</span>}<span style={{ fontSize: 12, color: "#555", fontFamily: "monospace" }}>{stateEvents.length} event{stateEvents.length !== 1 ? "s" : ""}</span></div>{stateEvents.map(e => <EventCard key={e.id} event={e} onEdit={onEdit} onDelete={onDelete} />)}</div>}{!selectedState && <div style={{ color: "#444", fontSize: 13, textAlign: "center", padding: "40px 0" }}>Select a state above to see its events</div>}</div>; }
function PressClipsTab({ events }) { const [filterCandidate, setFilterCandidate] = useState("all"); const allClips = useMemo(() => { const clips = []; events.forEach(e => { if (filterCandidate !== "all" && e.candidate !== filterCandidate) return; (e.press_clips || []).forEach(clip => { clips.push({ ...clip, candidate: e.candidate, eventTitle: e.title, eventDate: e.date, eventId: e.id }); }); }); return clips.sort((a, b) => (b.date || "").localeCompare(a.date || "")); }, [events, filterCandidate]); return <div><div style={{ display: "flex", gap: 10, marginBottom: 20, alignItems: "center" }}><select value={filterCandidate} onChange={e => setFilterCandidate(e.target.value)} style={{ ...S.input, width: "auto" }}><option value="all">All Candidates</option>{CANDIDATES.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><div style={{ fontSize: 12, color: "#555", fontFamily: "monospace" }}>{allClips.length} press clips</div></div>{allClips.length === 0 ? <div style={{ color: "#444", fontSize: 13 }}>No press clips tracked yet.</div> : allClips.map((clip, i) => { const c = CANDIDATE_MAP[clip.candidate]; return <div key={i} style={{ ...S.card, borderLeft: `3px solid ${c?.color || "#333"}` }}><div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 6, flexWrap: "wrap" }}><CandidateBadge id={clip.candidate} /><span style={{ fontSize: 11, color: "#555", fontFamily: "monospace" }}>{clip.date || clip.eventDate}</span><span style={{ fontSize: 11, color: "#666" }}>via: {clip.eventTitle}</span></div><div style={{ fontSize: 13, color: "#888", marginBottom: 4, fontWeight: 600 }}>{clip.outlet}</div>{clip.url ? <a href={clip.url} target="_blank" rel="noopener noreferrer" style={{ fontSize: 14, color: "#6a9bff", textDecoration: "none" }}>{clip.headline}</a> : <div style={{ fontSize: 14, color: "#ccc" }}>{clip.headline}</div>}</div>; })}</div>; }
export default function App() { const [activeTab, setActiveTab] = useState("Overview"); const [fetchedEvents, setFetchedEvents] = useState([]); const [manualEvents, setManualEvents] = useState(loadManualEvents); const [loading, setLoading] = useState(true); const [lastUpdated, setLastUpdated] = useState(""); const [editingEvent, setEditingEvent] = useState(null); const [showForm, setShowForm] = useState(false); useEffect(() => { fetch("/events.json").then(r => r.json()).then(data => { setFetchedEvents(data.events || []); setLastUpdated(data.last_updated || ""); }).catch(() => {}).finally(() => setLoading(false)); }, []); const allEvents = useMemo(() => { const fetchedIds = new Set(fetchedEvents.map(e => e.id)); const unique = [...fetchedEvents, ...manualEvents.filter(e => !fetchedIds.has(e.id))]; return unique.sort((a, b) => b.date.localeCompare(a.date)); }, [fetchedEvents, manualEvents]); function handleSaveEvent(event) { const updated = manualEvents.find(e => e.id === event.id) ? manualEvents.map(e => e.id === event.id ? event : e) : [...manualEvents, event]; setManualEvents(updated); saveManualEvents(updated); if (fetchedEvents.find(e => e.id === event.id)) { setFetchedEvents(prev => prev.map(e => e.id === event.id ? event : e)); } setEditingEvent(null); setShowForm(false); } function handleDeleteEvent(id) { if (!confirm("Delete this event?")) return; const updatedManual = manualEvents.filter(e => e.id !== id); setManualEvents(updatedManual); saveManualEvents(updatedManual); setFetchedEvents(prev => prev.filter(e => e.id !== id)); } function handleEdit(event) { setEditingEvent(event); setShowForm(true); window.scrollTo({ top: 0, behavior: "smooth" }); } function handleAddNew() { setEditingEvent(blankEvent()); setShowForm(true); window.scrollTo({ top: 0, behavior: "smooth" }); } return <div style={S.app}><div style={S.header}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}><div><h1 style={S.title}>2028 Democratic Primary · Event Tracker</h1><div style={S.subtitle}>{loading ? "Loading…" : `${allEvents.length} events tracked · ${lastUpdated ? "Updated " + lastUpdated.slice(0, 10) : "auto-updated daily"}`}</div></div><button onClick={handleAddNew} style={S.btn()}>+ Add Event</button></div></div><div style={S.tabs}>{TABS.map(tab => <button key={tab} onClick={() => setActiveTab(tab)} style={S.tab(activeTab === tab)}>{tab}</button>)}</div><div style={S.body}>{showForm && editingEvent && <EventForm event={editingEvent} onSave={handleSaveEvent} onCancel={() => { setShowForm(false); setEditingEvent(null); }} />}{loading ? <div style={{ color: "#555", fontFamily: "monospace" }}>Loading events…</div> : activeTab === "Overview" ? <OverviewTab events={allEvents} /> : activeTab === "All Events" ? <AllEventsTab events={allEvents} onEdit={handleEdit} onDelete={handleDeleteEvent} onAdd={handleAddNew} /> : activeTab === "By Candidate" ? <ByCandidateTab events={allEvents} onEdit={handleEdit} onDelete={handleDeleteEvent} onAdd={handleAddNew} /> : activeTab === "By State" ? <ByStateTab events={allEvents} onEdit={handleEdit} onDelete={handleDeleteEvent} /> : activeTab === "Press Clips" ? <PressClipsTab events={allEvents} /> : null}</div><div style={{ padding: "16px 36px 32px", borderTop: "1px solid #1a1a22", marginTop: 8 }}><div style={{ fontSize: 11, color: "#444", fontFamily: "monospace" }}>Events auto-fetched daily via GitHub Actions + Anthropic API · Manual edits stored in browser localStorage · No candidates formally declared as of March 2026</div></div></div>; }
